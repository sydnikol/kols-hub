# Using these as Notion Template Buttons

1. Import the CSV/MD files into the matching database or page.
2. In the database, click the down-arrow on the **New** button → **New template**.
3. Paste the content from the imported row/page into the template body.
4. Rename the template (e.g., "Health Day", "ChronoMuse Scene", "Outfit Suggestion").
5. Set it as default if you want it to auto-apply on new entries.
